# 生产环境建议 pip freeze 固定版本；此处为松散上下限，避免大版本升级踩雷
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<3.0.0
matplotlib>=3.7.0
mplfinance>=0.12.10b0
akshare>=1.12.0
plotly>=5.15.0
flask>=3.0.0,<4.0.0
markdown>=3.5.0
gunicorn>=22.0.0
requests>=2.31.0
python-binance>=1.0.19
pytest>=8.0.0
# ============================================================
# ML 推理依赖 (inference - 生产环境必需)
# ============================================================
lightgbm>=4.0.0
scikit-learn>=1.3.0
xgboost>=1.7.0
onnxruntime>=1.16.0
# torch>=2.0.0  # 训练环境安装；生产环境有 ONNX 可不装

# ============================================================
# ML 训练依赖 (training-only - 仅 H800 训练环境需要)
# ============================================================
# pytorch-tabnet>=4.0
# optuna>=3.0.0
# tensorboard>=2.14.0
